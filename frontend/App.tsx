/**
 * Expo entry point.
 *
 * Application orchestration lives under src/app so this file never becomes a
 * second feature container.
 */
import './global.css';

export { default } from './src/app/DestinyOneApp';
